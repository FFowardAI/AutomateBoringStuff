# Supabase Configuration
SUPABASE_URL=""
SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY="" # If using service role for backend operations

# Server Configuration
PORT=8002 # Use a different port than the old server

# Optional: API Keys for external services (VLM, Compute, etc.)
# VLM_API_KEY=""
# ANTHROPIC_API_KEY="" # Required for the VLM feature using Anthropic Claude API
# COMPUTE_SERVICE_URL="" 